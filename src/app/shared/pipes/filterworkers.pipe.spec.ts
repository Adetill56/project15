import { FilterworkersPipe } from './filterworkers.pipe';

describe('FilterworkersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterworkersPipe();
    expect(pipe).toBeTruthy();
  });
});
